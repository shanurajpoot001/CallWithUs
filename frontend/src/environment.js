// Hardcoded server URL to avoid Render environment variable issues
const server = "https://callwithus-backend.onrender.com";

console.log('=== HARDCODED SERVER URL ===');
console.log('Server URL:', server);
console.log('============================');

export default server;